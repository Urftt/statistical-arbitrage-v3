---
phase: 03-backtest-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/pair-analysis/BacktestTab.tsx
  - frontend/src/app/(dashboard)/pair-analysis/page.tsx
autonomous: true
requirements: [BT-01, BT-02, BT-06, BT-07, BT-08, BT-09, BT-10, UX-02, UX-04]

must_haves:
  truths:
    - "User can see 7 strategy parameter sliders grouped into Signal, Risk Management, and Execution sections"
    - "User can click Run Backtest and see a loading skeleton during computation"
    - "User can click Reset to Defaults and all sliders return to DEFAULT_STRATEGY_PARAMETERS values"
    - "User can view 6 metric cards (Sharpe, Max Drawdown, Win Rate, Total P&L, Total Trades, Final Equity) with colored badges"
    - "User can view a trade log table with direction, timestamps, bars held, exit reason, net P&L, and return"
    - "User sees preflight blocker alerts that prevent results from rendering"
    - "User sees preflight warning alerts above results"
    - "User sees overfitting warning banners between metric cards and charts placeholder"
    - "User can expand Assumptions & Limitations accordion showing footer content"
    - "Results display shows 'Generated for ASSET1/ASSET2' context label"
  artifacts:
    - path: "frontend/src/components/pair-analysis/BacktestTab.tsx"
      provides: "BacktestTab component with parameter form, metrics, trade log, warnings, and accordion"
      min_lines: 200
    - path: "frontend/src/app/(dashboard)/pair-analysis/page.tsx"
      provides: "Wires BacktestTab into the backtest tab panel"
      contains: "BacktestTab"
  key_links:
    - from: "frontend/src/components/pair-analysis/BacktestTab.tsx"
      to: "frontend/src/lib/api.ts"
      via: "postBacktest() and postCointegration() imports"
      pattern: "import.*postBacktest.*from.*api"
    - from: "frontend/src/components/pair-analysis/BacktestTab.tsx"
      to: "frontend/src/contexts/PairContext.tsx"
      via: "usePairContext() for asset1, asset2, timeframe"
      pattern: "usePairContext"
    - from: "frontend/src/app/(dashboard)/pair-analysis/page.tsx"
      to: "frontend/src/components/pair-analysis/BacktestTab.tsx"
      via: "import and JSX usage in tab panel"
      pattern: "import BacktestTab"
---

<objective>
Build the BacktestTab component with parameter form, API wiring, metric cards, trade log, warning display hierarchy, and Assumptions & Limitations accordion. Wire it into the Pair Analysis page.

Purpose: Delivers the core backtest interaction loop -- users configure parameters, run a backtest, and see quantitative results (metrics, trade log, warnings, honest reporting). Charts are added in Plan 02.
Output: Working BacktestTab.tsx with form + results (minus charts), wired into page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtest-tab/03-CONTEXT.md
@.planning/phases/03-backtest-tab/03-RESEARCH.md
@.planning/phases/03-backtest-tab/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/lib/api.ts:
```typescript
export interface StrategyParametersPayload {
  lookback_window: number;
  entry_threshold: number;
  exit_threshold: number;
  stop_loss: number;
  initial_capital: number;
  position_size: number;
  transaction_fee: number;
  min_trade_count_warning: number;
}

export const DEFAULT_STRATEGY_PARAMETERS: StrategyParametersPayload = {
  lookback_window: 60,
  entry_threshold: 2,
  exit_threshold: 0.5,
  stop_loss: 3,
  initial_capital: 10_000,
  position_size: 0.5,
  transaction_fee: 0.0025,
  min_trade_count_warning: 3,
};

export interface BacktestRequest {
  asset1: string;
  asset2: string;
  timeframe: string;
  days_back: number;
  strategy: StrategyParametersPayload;
}

export interface BacktestResponse {
  status: 'ok' | 'blocked';
  request: BacktestRequest;
  data_quality: DataQualityReportPayload;
  warnings: EngineWarningPayload[];
  footer: HonestReportingFooterPayload;
  signal_overlay: SignalOverlayPointPayload[];
  trade_log: TradeLogEntryPayload[];
  equity_curve: EquityCurvePointPayload[];
  metrics: MetricSummaryPayload;
  spread_summary: SpreadSummaryPayload;
}

export interface MetricSummaryPayload {
  total_trades: number;
  winning_trades: number;
  losing_trades: number;
  win_rate: number;
  total_net_pnl: number;
  total_return_pct: number;
  average_trade_return_pct: number;
  average_holding_period_bars: number;
  max_drawdown_pct: number;
  profit_factor: number | null;
  sharpe_ratio: number | null;
  sortino_ratio: number | null;
  final_equity: number;
}

export interface DataQualityReportPayload {
  status: 'passed' | 'blocked';
  observations_total: number;
  observations_usable: number;
  warmup_bars: number;
  blockers: EngineWarningPayload[];
  warnings: EngineWarningPayload[];
}

export interface EngineWarningPayload {
  code: string;
  severity: 'warning' | 'blocking';
  message: string;
  details: Record<string, number | string | boolean | null | undefined>;
}

export interface HonestReportingFooterPayload {
  execution_model: string;
  fee_model: string;
  data_basis: string;
  assumptions: string[];
  limitations: string[];
}

export interface TradeLogEntryPayload {
  trade_id: number;
  direction: 'long_spread' | 'short_spread';
  entry_timestamp: string;
  exit_timestamp: string;
  exit_reason: 'long_exit' | 'short_exit' | 'stop_loss';
  bars_held: number;
  net_pnl: number;
  return_pct: number;
  // ... additional fields available but not needed for table display
}

export interface CointegrationResponse {
  spread: (number | null)[];
  zscore: (number | null)[];
  timestamps: number[];
  // ... additional fields
}

export async function postBacktest(req: BacktestRequest): Promise<BacktestResponse>;
export async function postCointegration(req: CointegrationRequest): Promise<CointegrationResponse>;
```

From frontend/src/contexts/PairContext.tsx:
```typescript
export function usePairContext(): { asset1: string; asset2: string; timeframe: string; ... };
```

From frontend/src/app/(dashboard)/pair-analysis/page.tsx (line 103-105):
```typescript
<Tabs.Panel value="backtest" pt="md">
  <Text c="dimmed">Backtest — coming in Phase 3</Text>
</Tabs.Panel>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BacktestTab component with parameter form, API call, metrics, trade log, warnings, and accordion</name>
  <files>frontend/src/components/pair-analysis/BacktestTab.tsx</files>
  <read_first>
    - frontend/src/components/pair-analysis/StatisticsTab.tsx (established tab pattern: useState, badge helpers, Mantine layout, PlotlyChart usage, Slider usage)
    - frontend/src/lib/api.ts (lines 22-244: all backtest interfaces, DEFAULT_STRATEGY_PARAMETERS, postBacktest, postCointegration)
    - frontend/src/contexts/PairContext.tsx (usePairContext hook signature)
    - .planning/phases/03-backtest-tab/03-UI-SPEC.md (slider ranges, badge thresholds, trade log columns, copywriting contract)
    - .planning/phases/03-backtest-tab/03-RESEARCH.md (Pattern 1: click-triggered fetch, Pattern 2: pair-change clear, Pattern 5: three-tier warning hierarchy)
  </read_first>
  <action>
Create `frontend/src/components/pair-analysis/BacktestTab.tsx` as a `'use client'` component. Follow the StatisticsTab.tsx pattern for structure.

**State management:**
- `const [params, setParams] = useState<StrategyParametersPayload>({...DEFAULT_STRATEGY_PARAMETERS})` for slider state
- `const [loading, setLoading] = useState(false)` for fetch status
- `const [error, setError] = useState<string | null>(null)` for API errors
- `const [data, setData] = useState<BacktestResponse | null>(null)` for backtest results
- `const [cointData, setCointData] = useState<CointegrationResponse | null>(null)` for spread/zscore series (needed by Plan 02 charts)
- `const cancelRef = useRef<boolean>(false)` for in-flight cancellation

**Pair-change clear (per D-05, NAV-05):**
```typescript
useEffect(() => {
  cancelRef.current = true;
  setData(null);
  setCointData(null);
  setError(null);
  setParams({...DEFAULT_STRATEGY_PARAMETERS});
}, [asset1, asset2, timeframe]);
```

**handleRun function (click-triggered, NOT auto-fetch per D-05):**
- Set `cancelRef.current = false`, `setLoading(true)`, `setError(null)`
- Build request: `{ asset1: \`${asset1}/EUR\`, asset2: \`${asset2}/EUR\`, timeframe, days_back: 365, strategy: { ...params, min_trade_count_warning: 3 } }`
- Build cointegration request: `{ asset1: \`${asset1}/EUR\`, asset2: \`${asset2}/EUR\`, timeframe, days_back: 365 }`
- Use `Promise.all([postBacktest(btReq), postCointegration(cointReq)])` to fetch both in parallel
- On success: if `!cancelRef.current`, set both `data` and `cointData`
- On error: if `!cancelRef.current`, set error with message from caught Error
- In finally: if `!cancelRef.current`, `setLoading(false)`

**handleReset function (per D-04):**
- `setParams({...DEFAULT_STRATEGY_PARAMETERS})`
- `setData(null)`
- `setCointData(null)`
- `setError(null)`

**Parameter form layout (per D-01, D-02, D-03 -- always visible above results):**
Three grouped sections using `Stack gap="lg"`:

Section "Signal" (`Text size="sm" fw={600}`):
- Lookback window: `Slider min={20} max={365} step={5}` — label shows `{n} bars`
- Entry threshold: `Slider min={1.0} max={4.0} step={0.1}` — label shows `{n}σ`
- Exit threshold: `Slider min={0.0} max={2.0} step={0.1}` — label shows `{n}σ`

Section "Risk Management":
- Stop-loss: `Slider min={1.0} max={6.0} step={0.1}` — label shows `{n}σ`
- Position size: `Slider min={0.1} max={1.0} step={0.05}` — label shows `{(n*100).toFixed(0)}%`

Section "Execution":
- Initial capital: `Slider min={1000} max={100000} step={1000}` — label shows `€{n.toLocaleString()}`
- Transaction fee: `Slider min={0} max={0.01} step={0.0005}` — label shows `{(n*100).toFixed(2)}%`

Each slider row: `Group justify="space-between"` with `Text size="sm"` for label name on left, `Text size="xs" c="dimmed"` for current value on right, `Slider` below.

**Button row (per D-04):**
`Group mt="md"`:
- `Button variant="filled" color="blue" onClick={handleRun} disabled={loading} leftSection={loading ? <Loader size="xs" /> : <IconPlayerPlay size={16} />}` with text "Run Backtest"
- `Button variant="default" onClick={handleReset} leftSection={<IconRefresh size={16} />}` with text "Reset to Defaults"

**Error state:**
If `error` is set, render `Alert color="red" icon={<IconAlertTriangle size={16} />}` with text: `Backtest failed — ${error}. Check that both assets have cached data and try again.`

**Empty state (no data, no loading, no error):**
Render `Paper p="xl" ta="center"`:
- `Text size="lg" fw={600}` with "Configure and run a backtest"
- `Text size="sm" c="dimmed"` with "Set your strategy parameters above and click Run Backtest to see equity curve, metrics, and trade log."

**Loading state (per UX-02):**
When `loading` is true, render a Skeleton stack matching the results layout:
- `Skeleton height={28}` for context label
- `SimpleGrid cols={{ base: 2, sm: 3, lg: 6 }}` with 6x `Skeleton height={88}`
- `Skeleton height={280}` (equity curve placeholder)
- `Skeleton height={180}` (drawdown placeholder)
- `Skeleton height={300}` (z-score placeholder)
- `Skeleton height={260}` (spread placeholder)
- `Skeleton height={200}` (trade log placeholder)
- `Text size="sm" c="dimmed" ta="center"` with "Running backtest..."

**Results section (when data is set and not loading):**

1. **Preflight blocker check (per D-11):** If `data.status === 'blocked'`, render ONLY blocker alerts and return. Each blocker from `data.data_quality.blockers`: `Alert color="red" icon={<IconAlertTriangle size={16} />} title="Blocked"` with `{blocker.message}`.

2. **Preflight warnings (per D-11):** If `data.data_quality.warnings.length > 0`, render each warning as `Alert color="yellow" icon={<IconAlertTriangle size={16} />}` with `{warning.message}`.

3. **Context label (per D-08, UX-04):** `Text size="xs" c="dimmed"` with `Generated for ${asset1}/${asset2}`.

4. **Metric cards (per D-07):** `SimpleGrid cols={{ base: 2, sm: 3, lg: 6 }} spacing="md"` with 6 `Paper p="md"` cards:
   - Sharpe Ratio: value `data.metrics.sharpe_ratio?.toFixed(2) ?? 'N/A'`, badge: green if >= 1.5, yellow if >= 0.5, red if < 0.5. `Tooltip` on this card shows `Sortino: ${data.metrics.sortino_ratio?.toFixed(2) ?? 'N/A'}`.
   - Max Drawdown: value `${data.metrics.max_drawdown_pct.toFixed(1)}%`, badge: green if < 10, yellow if <= 25, red if > 25.
   - Win Rate: value `${(data.metrics.win_rate * 100).toFixed(1)}%`, badge: green if > 0.55, yellow if >= 0.40, red if < 0.40. `Tooltip` shows `Profit Factor: ${data.metrics.profit_factor?.toFixed(2) ?? 'N/A'}`.
   - Total P&L: value `€${data.metrics.total_net_pnl.toFixed(2)}`, badge: green if > 0, red if <= 0.
   - Total Trades: value `${data.metrics.total_trades}`, badge: green if >= 10, yellow if >= 3, red if < 3. `Tooltip` shows `Avg Return: ${data.metrics.average_trade_return_pct.toFixed(2)}% | Avg Holding: ${data.metrics.average_holding_period_bars} bars`.
   - Final Equity: value `€${data.metrics.final_equity.toFixed(2)}`, badge: green if > params.initial_capital, red if <= params.initial_capital.

   Each card structure: `Paper p="md"` containing `Stack gap="xs"` with `Text size="xs" c="dimmed"` label, `Text size="xl" fw={600}` value, and `Badge` with color and label from threshold logic.

   Create badge helper functions at the top of the file (outside the component, like StatisticsTab does):
   - `function sharpeBadge(v: number | null): { color: string; label: string }`
   - `function drawdownBadge(v: number): { color: string; label: string }`
   - `function winRateBadge(v: number): { color: string; label: string }`
   - `function pnlBadge(v: number): { color: string; label: string }`
   - `function tradeCountBadge(v: number): { color: string; label: string }`
   - `function equityBadge(v: number, initial: number): { color: string; label: string }`

5. **Overfitting warnings (per D-12):** If `data.warnings.length > 0`, render `Alert color="yellow" icon={<IconAlertTriangle size={16} />} title="Results may be misleading"` with a bulleted list (`List` or manual `ul`) of each `data.warnings[].message`.

6. **Chart placeholders:** Render `Text size="sm" c="dimmed"` placeholders saying "Charts will render here" for the 4 chart slots. Plan 02 will replace these with actual PlotlyChart components. The placeholder order is: equity curve, drawdown, z-score, spread.

7. **No-trades check:** If `data.trade_log.length === 0`, render `Alert color="yellow"` with "No trades executed. Try widening the entry threshold or increasing the lookback window." instead of the trade log.

8. **Trade log table (per D-07, BT-07):** `Table striped highlightOnHover` with columns in this order:
   | Header | Field | Format |
   |--------|-------|--------|
   | # | trade_id | integer |
   | Direction | direction | Badge: `long_spread` -> green "Long", `short_spread` -> red "Short" |
   | Entry | entry_timestamp | `new Date(entry_timestamp).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' })` |
   | Exit | exit_timestamp | same format as Entry |
   | Bars | bars_held | integer |
   | Exit Reason | exit_reason | `long_exit`/`short_exit` -> "Mean Rev.", `stop_loss` -> orange Badge "Stop Loss" |
   | Net P&L | net_pnl | `€${net_pnl.toFixed(2)}`, green text (`c="green"`) if > 0, red text (`c="red"`) if <= 0 |
   | Return | return_pct | `${return_pct.toFixed(2)}%`, same coloring as P&L |

   If `data.trade_log.length > 50`, show only first 50 rows and add `Text size="xs" c="dimmed"` below: `Showing first 50 of ${data.trade_log.length} trades.`

9. **Assumptions & Limitations accordion (per D-13, BT-09):** `Accordion variant="contained" radius="sm"` with one item `value="assumptions"`:
   - `Accordion.Control`: "Assumptions & Limitations"
   - `Accordion.Panel`: `Stack gap="sm"` containing:
     - `Text size="sm" fw={600}` "Execution Model" + `Text size="sm"` `{data.footer.execution_model}`
     - `Text size="sm" fw={600}` "Fee Model" + `Text size="sm"` `{data.footer.fee_model}`
     - `Text size="sm" fw={600}` "Data Basis" + `Text size="sm"` `{data.footer.data_basis}`
     - `Text size="sm" fw={600}` "Assumptions" + bulleted list of `data.footer.assumptions[]`
     - `Text size="sm" fw={600}` "Limitations" + bulleted list of `data.footer.limitations[]`

**Imports needed:**
- `{ useState, useEffect, useRef }` from `'react'`
- `{ Stack, Group, Text, Paper, SimpleGrid, Badge, Button, Slider, Alert, Accordion, Table, Skeleton, Tooltip, Loader, List }` from `'@mantine/core'`
- `{ IconPlayerPlay, IconRefresh, IconAlertTriangle }` from `'@tabler/icons-react'`
- `{ postBacktest, postCointegration, DEFAULT_STRATEGY_PARAMETERS, type BacktestResponse, type CointegrationResponse, type StrategyParametersPayload }` from `'@/lib/api'`
- `{ usePairContext }` from `'@/contexts/PairContext'`

**CRITICAL: Do NOT expose `min_trade_count_warning` as a slider.** Always send it as `3` in the request. Only 7 sliders: lookback_window, entry_threshold, exit_threshold, stop_loss, position_size, initial_capital, transaction_fee.

**Export:** `export default function BacktestTab()`
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/statistical-arbitrage-v3/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/pair-analysis/BacktestTab.tsx exists and starts with `'use client'`
    - BacktestTab.tsx contains `export default function BacktestTab`
    - BacktestTab.tsx contains `import.*postBacktest.*from.*@/lib/api`
    - BacktestTab.tsx contains `import.*postCointegration.*from.*@/lib/api`
    - BacktestTab.tsx contains `import.*usePairContext.*from.*@/contexts/PairContext`
    - BacktestTab.tsx contains `useState<StrategyParametersPayload>`
    - BacktestTab.tsx contains `DEFAULT_STRATEGY_PARAMETERS`
    - BacktestTab.tsx contains `Promise.all` (parallel fetch of backtest + cointegration)
    - BacktestTab.tsx contains `cancelRef` (useRef for cancellation)
    - BacktestTab.tsx contains 7 `<Slider` elements (one per parameter)
    - BacktestTab.tsx contains `min_trade_count_warning: 3` (hardcoded, not a slider)
    - BacktestTab.tsx contains `Run Backtest` and `Reset to Defaults` button text
    - BacktestTab.tsx contains `SimpleGrid` with 6 metric cards
    - BacktestTab.tsx contains `sharpeBadge` and `drawdownBadge` helper functions
    - BacktestTab.tsx contains `Tooltip` for secondary metrics (Sortino, profit factor)
    - BacktestTab.tsx contains `data.status === 'blocked'` check that prevents results rendering
    - BacktestTab.tsx contains `data.data_quality.warnings` for preflight warning display
    - BacktestTab.tsx contains `data.warnings` for overfitting warning display
    - BacktestTab.tsx contains `Results may be misleading` (overfitting warning title)
    - BacktestTab.tsx contains `Generated for` (UX-04 context label)
    - BacktestTab.tsx contains `<Table` for trade log
    - BacktestTab.tsx contains `Accordion` for Assumptions & Limitations
    - BacktestTab.tsx contains `data.footer.execution_model` and `data.footer.assumptions`
    - BacktestTab.tsx contains `Showing first 50` (trade log truncation logic)
    - BacktestTab.tsx contains `No trades executed` (empty trade log message)
    - BacktestTab.tsx contains `Skeleton` components for loading state
    - BacktestTab.tsx contains `Running backtest` loading text
    - BacktestTab.tsx contains `useEffect` keyed on asset1/asset2/timeframe that clears state
    - TypeScript compilation passes: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>BacktestTab.tsx exists with parameter form (7 sliders in 3 groups), Run/Reset buttons, click-triggered parallel API fetch, loading skeleton, error state, blocker/warning/overfitting alert hierarchy, 6 metric cards with colored badges and tooltip secondary metrics, trade log table (truncated at 50), and Assumptions accordion. Charts are placeholder text (Plan 02). TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BacktestTab into pair-analysis page</name>
  <files>frontend/src/app/(dashboard)/pair-analysis/page.tsx</files>
  <read_first>
    - frontend/src/app/(dashboard)/pair-analysis/page.tsx (current tab panel placeholders)
    - frontend/src/components/pair-analysis/BacktestTab.tsx (just created in Task 1)
  </read_first>
  <action>
In `frontend/src/app/(dashboard)/pair-analysis/page.tsx`:

1. Add import at the top (after the StatisticsTab import on line 14):
   ```typescript
   import BacktestTab from '@/components/pair-analysis/BacktestTab';
   ```

2. Replace the backtest tab panel placeholder (line 103-105):
   Change:
   ```typescript
   <Tabs.Panel value="backtest" pt="md">
     <Text c="dimmed">Backtest — coming in Phase 3</Text>
   </Tabs.Panel>
   ```
   To:
   ```typescript
   <Tabs.Panel value="backtest" pt="md">
     <BacktestTab />
   </Tabs.Panel>
   ```
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/statistical-arbitrage-v3/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `import BacktestTab from '@/components/pair-analysis/BacktestTab'`
    - page.tsx contains `<BacktestTab />` inside the backtest Tabs.Panel
    - page.tsx does NOT contain `Backtest — coming in Phase 3`
    - TypeScript compilation passes: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>BacktestTab is imported and rendered in the backtest tab panel. The placeholder text is removed. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes (no TypeScript errors)
- `cd frontend && npm run lint` passes (no ESLint errors)
- BacktestTab.tsx is a well-structured component following StatisticsTab.tsx patterns
- All 12 requirement IDs addressed by this plan's tasks are traceable to specific code sections
</verification>

<success_criteria>
- BacktestTab renders in the Pair Analysis page under the Backtest tab
- 7 sliders organized in 3 groups allow parameter configuration
- Run Backtest calls postBacktest + postCointegration in parallel
- Loading skeleton displays during computation
- Metric cards show with colored badge interpretation
- Trade log table renders with correct column format
- Preflight blockers prevent results rendering
- Overfitting warnings appear between cards and chart placeholders
- Assumptions accordion is collapsed by default, expandable
- "Generated for" label shows the current pair
- Pair change clears all results and resets sliders
</success_criteria>

<output>
After completion, create `.planning/phases/03-backtest-tab/03-01-SUMMARY.md`
</output>
